<Page x:Class="Pr1.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <TextBlock FontSize="25">y = sin^2 x + cos^5 x^3 + In^2/5</TextBlock>
            <TextBlock x:Name="txtY" FontSize="25">y = </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="25">Z = </TextBlock>
                <TextBox x:Name="txtz" KeyDown="txtz_KeyDown" FontSize="25" MinWidth="50" TextChanged="txtX_TextChanged" ></TextBox>

                <StackPanel Margin="150,0,0,0" Orientation="Horizontal">
                    <TextBlock x:Name="txtx" FontSize="25"></TextBlock>
                </StackPanel>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Page>
